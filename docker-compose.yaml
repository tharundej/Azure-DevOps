version: "3.9"
services:
  node_frontend:
    build: 
    image: ui_erp:anil-${BUILD_NUMBER}
    container_name: node_frontend
    environment:
        - TZ=Asia/Kolkata
    volumes:
      - '.:/app'
      - '/app/node_modules'
      - "/etc/timezone:/etc/timezone:ro" 
      - "/etc/localtime:/etc/localtime:ro"
    ports:
      - 3000:3000
    networks:
      - default

    restart: always